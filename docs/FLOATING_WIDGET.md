# 用量悬浮窗

本次实现位于 `codex/quota-floating-widget`，基于 `main` 的 `b7eef3d`。原 `dev` 工作目录中的未提交修改未改动。

## 交互

- 启动时显示深色悬浮胶囊，右侧拖动柄可移动位置。主窗口侧栏也可重新打开悬浮窗。
- 鼠标停留 160ms 展开；离开 350ms 后收起。点击「固定」保持展开；Escape 取消固定并收起。键盘聚焦入口或按 Enter 也可展开。
- 展开后先切换 `Window View` / `Wallet View`，再从横向供应商简称列表中选择一项，下方只展示该供应商在当前分类中的内容。窗口与余额分别筛选，两个分类在本次运行中各自记住选择。
- 当前分类只列出已有该类额度的供应商；选中的供应商移除后自动回退到首个可用项，分类为空时显示空状态。其他状态或计数提示本视图暂不展示。
- 浮窗窗口额度按同一账户/模型的 5 小时、周窗口顺序显示；主窗口仍保持原排序。长列表可滚动，周额度耗尽时，同一账户/模型的短周期继续显示受限。
- 悬停只读取本机最近快照，两秒内的重复悬停合并；「刷新用量」才请求现有 Provider 查询。缓存、离线、部分失败和结果未知保留明确提示。
- 「主窗口」恢复账户管理界面；「隐藏」返回主窗口并隐藏胶囊。关闭主窗口会隐藏主窗口并显示胶囊；系统关闭浮窗（含 Cmd+W）会恢复主窗口并隐藏浮窗，保留其再次打开的能力；macOS Dock 重开恢复主窗口；退出仍使用应用菜单 / Cmd+Q。
- 刷新以及添加、更换、删除账户后，通过不含账户正文的事件提示另一窗口重读本机投影。刷新与账户变更共用 Host 事务锁，忙碌时拒绝重叠操作。

## 数据边界

平台、账户额度沿用现有 DTO；只有上游提供模型明细时才展示模型名称。不同平台的百分比和币种不合计。进度条始终表示剩余比例，余额和不限量值不转换为百分比。当前版本仍是按需刷新，不能视为实时监控。

窗口采用 Tauri 原生透明、无边框、置顶窗口，实际尺寸收起为 264×72，展开为 384×536 逻辑像素。尺寸变化会限制在当前屏幕工作区内。macOS 透明配置开启了 Tauri `macos-private-api`；后续若改为 Mac App Store 分发需重新评估该实现，见 [Tauri 配置说明](https://v2.tauri.app/reference/config/#windowconfig)。

## 验证与限制（2026-09-22）

- 48 项前端单元测试通过，涵盖分类与供应商双重筛选、分别记住选择、选中项消失后的回退、空分类、切换零 Host 请求，以及已有悬停、额度语义和同步测试。
- 6 项浏览器 E2E 通过，涵盖主界面回归、两级选择、5 小时/周排序、余额不混入窗口、键盘选择、悬停、固定、Escape、空账户、离线和未知结果；已查看 Window View 与 Wallet View 的实际截图。
- TypeScript、ESLint、Renderer 边界、生产前端构建与 fixture 排除检查通过。
- Pen 保存后重新打开检查通过；原有 141 个顶层节点的完整子树与 Git 基线逐项摘要对比一致。新增 9 个状态画板及 2 个说明画板，已查看状态截图；仅离线画板的滚动区存在预期的底部裁切。
- 2026-09-22 修复系统关闭浮窗后不可重新打开的问题，Rust 37 项测试、格式检查和 Clippy 通过。本机使用已有 Rust 1.98.1，而 CI 固定版本为 1.97.1。未设置真实 Python integration 环境变量，带可选外部环境的 Rust 测试不能据此声称实际 Sidecar 集成已验证。
- 编译了开发 Host 并尝试运行独立空账户预览。原生 UI 自动化工具超时且未找到预览进程对应的应用，未完成透明置顶、原生悬停拖动、多显示器、关闭恢复或 Cmd+Q 的运行验收。
- 本功能分支交付代码、可编辑 Pen 原型、原型预览图及本说明；尚未替换已安装应用、未查询真实 Provider、未发布安装包或合并主分支。浏览器截图和原型均使用演示数据。

## 原型与开发交接

原型位于 [agent-quota-desktop.pen](design/agent-quota-desktop.pen) 的 `17.x Floating` 模块（画布底部，从 `x=0, y=27880` 开始），包含 9 个状态画板和 2 个说明画板。文字、按钮、进度条和容器为独立可编辑图层。参见 [状态索引与预览](design/floating-usage/README.md)。

实现入口为 `src/ui/FloatingQuota.tsx`，窗口桥接为 `src/host/floatingWindow.ts`，原生窗口配置与生命周期在 `src-tauri/tauri.conf.json` / `src-tauri/src/lib.rs`。`src/ui/FloatingQuota.test.tsx` 与 `tests/e2e/floating.spec.ts` 验证两级选择、用量语义和交互。UI 仍沿用现有 Host DTO，不引入新的业务命令或凭证入口。

## 本地查看

在本工作区执行 `VITE_AQ_FIXTURE_MODE=1 pnpm dev`，浏览器打开 `http://127.0.0.1:1420/?surface=floating&scenario=floating`。这只预览交互，不能验证桌面置顶或拖动。

生产版本仍应沿用仓库现有构建、资源校验及安装验收流程。本次开发预览不作为可分发安装包。
